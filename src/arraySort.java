import java.util.Arrays;

public class arraySort {
    public static void main(String[] args) {
        int[] arr = {56,-1,37,4,-10};
        // TIM Sort
        // Array Sorting using Inbuilt Function
        Arrays.sort(arr);
        System.out.println(Arrays.toString(arr));
    }
}
